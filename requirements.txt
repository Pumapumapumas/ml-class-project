# Pinned dependencies for the ml-class-project Telugu OCR pipeline.
# Install with: pip install -r requirements.txt
# Add new deps with a pin (>= for minimum, or == for exact).
# Treat this file as the source of truth for what's needed to reproduce.

# === Core tooling ===
python-dotenv>=1.0.0       # .env loading

# === Image processing ===
opencv-python>=4.9.0
Pillow>=10.0.0
scikit-image>=0.22.0
numpy>=1.26.0
deskew>=1.5.0              # skew angle detection for Phase 2 preprocessing

# === OCR (open-source backends) ===
pytesseract>=0.3.10        # Python wrapper around the Tesseract binary (runs via Docker — see environment_standard.md)
# surya-ocr>=0.6.0         # multilingual OCR (specialized for Indic); ~2-5 GB model weights — add when Phase 3 begins

# === LLM clients (vision-capable OCR) ===
google-generativeai>=0.7.0  # Gemini
# openai>=1.30.0            # GPT-4o (paid; uncomment when wiring it in)
# anthropic>=0.30.0         # Claude (paid; uncomment when wiring it in)

# === Dataset and evaluation ===
huggingface-hub>=0.23.0
jiwer>=3.0.0                # CER / WER

# === Reporting / analysis ===
pandas>=2.1.0
matplotlib>=3.8.0
seaborn>=0.13.0

# === Dev tooling ===
pytest>=8.0.0
pytest-cov>=4.1.0
ruff>=0.5.0

# === Jupyter (notebooks) ===
jupyterlab>=4.0.0
ipykernel>=6.29.0
